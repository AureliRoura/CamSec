; CamSec Multi-project workspace
;
; Subprojects:
;   - camara/     → ESP32-CAM firmware (main project)
;   - server/     → Docker stack (separate)
;
; To build the firmware:
;   cd camara && pio run
;
; To upload to ESP32-CAM:
;   cd camara && pio run -t upload
